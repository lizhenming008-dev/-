# 自动化维护记录

这份记录用于保存每日简报相关自动化的排查结论、修复动作和后续注意事项，避免每次从零开始查。

## 2026-05-26 新闻联播飞书推送

- 任务：飞书推送新闻联播深度解读到群「阿茗每日综合晚报」。
- 目标群：`oc_af59a0aeb3ba607bd7de1ebccb0be2ea`
- 当天日期：`20260526`，按 `Asia/Shanghai` 计算。
- 新闻联播目标页：`https://lizhenming008-dev.github.io/daily-briefing/%E6%96%B0%E9%97%BB%E8%81%94%E6%92%AD_20260526.html`
- 检查结果：目标页线上返回 `HTTP 404`，所以按规则发送首页兜底。
- 首页：`https://lizhenming008-dev.github.io/daily-briefing/`
- 图片检查：首页 HTML 中 `<img` / `class="news-img"` 统计为 `0`，需要在消息里提示“图片数量偏少，需检查生成任务”。
- 飞书发送结果：沙盒外执行 `lark-cli im +messages-send --as bot ...` 成功。
- 飞书消息 ID：`om_x100b6e6441b01884b250909dcaa1889`

## 已定位问题

- `lark-cli` 在沙盒内会报 `keychain not initialized`。
- 沙盒外执行 `lark-cli doctor` 可以通过，说明本机配置和 token 是可用的，问题是沙盒读取不到 macOS keychain。
- 后续遇到同类问题，直接用沙盒外执行重试同一条 `lark-cli im +messages-send` 命令。
- `lark-cli update` 自带更新器访问 `https://registry.npmjs.org/@larksuite/cli/latest` 会超时。
- 已改用 `npm install -g @larksuite/cli@latest --prefix /Users/lgggg/.hermes/node` 完成更新。
- 当前 `lark-cli` 版本：`1.0.40`。
- 更新后沙盒外 `lark-cli doctor` 通过，bot/user 身份均 ready；自检里的更新检查 warn 不影响飞书发送。

## 栏目隔离规则

- 新闻联播推送只允许发送 `新闻联播_YYYYMMDD.html` 或首页兜底。
- 禁止为了避免 404 改发 `AI热点_*`、`每日综合晚报_*` 或其他栏目直达链接。
- AI 早报、AI 晚报和新闻联播各走各自自动化，飞书消息标题、链接和说明必须匹配栏目。
- 已把栏目隔离规则写入新闻联播、AI 早报、AI 晚报三个推送自动化。

## 图片问题初查

- 当前本地 `新闻联播_20260520.html` 只有 1 个 `<img>`，且是 `data:image/jpeg;base64` 内嵌图。
- 当前本地 `新闻联播_20260521.html` 没有 `<img>`。
- 新闻联播页面缺图不是简单的远程图片 404，而是生成任务本身没有稳定产出足够图片。
- 后续修复方向：新闻联播生成任务应为每个主要卡片生成或引用可访问图片，并在发布前检查 `<img>` 数量不少于 3，且避免过长或不稳定的 base64 内嵌图。

## 2026-05-26 调整记录

- 更新 `lark-cli`：从 `1.0.33` 更新到 `1.0.40`。
- 调整新闻联播推送自动化：只允许新闻联播直达链接或首页兜底；遇到 keychain 问题用沙盒外执行；每次运行必须写 memory。
- 调整 AI 早报和 AI 晚报推送自动化：只允许对应 AI 页面或首页兜底；禁止改发新闻联播/综合晚报；遇到 keychain 问题用沙盒外执行；每次运行必须写各自 memory。
- 新增本维护记录文件，后续排查以这里和各 automation memory 为准。
- 修复旧新闻联播页面图片显示：`新闻联播_20260520.html` 增加 3 个稳定 `news-img` 视觉块并隐藏旧 base64 JPEG；`新闻联播_20260521.html` 增加 3 个稳定 `news-img` 视觉块。
- 已提交并推送：`500dfa5 Fix news briefing image fallbacks`。
- 线上 Pages 验证通过：`20260520` 图片统计 total=4，`20260521` 图片统计 total=3。
- 检查 2026-05-25 飞书发布同步：AI 早报已发布并修复链接，新闻联播未发布；`新闻联播_20260525.html` 线上为 `HTTP 404`，按栏目隔离规则补发首页兜底，不混用 AI 链接。
- 补发飞书消息 ID：`om_x100b6e6416d3b114b24ab890ec92d33`，已回读群消息确认存在。

## 每次运行必须记录

- 检查日期和目标 URL。
- 目标 URL 的 HTTP 状态。
- 实际发送的链接。
- 图片统计结果。
- 飞书发送结果和 message_id。
- 如果失败，记录失败原因和下一步处理方式。
